<% content_for :page_title, t('.title') %>
<% content_for :page_subtitle, t('.subtitle') %>
<% content_for :page_view, render_page_view(__FILE__) %>

<%= form_for @account, url: @post_path, html:{autocomplete: "on"} ,method: :post, local: true do |f| %>
  <%= form_errors_for [@account, @address_summary, @address_detail] %>

  <%= render "/layouts/address" %>
  <%= f.button 'next', { :name => 'submitted' }  %>
<% end %>