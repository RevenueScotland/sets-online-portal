    <%#%= c.gds_text_field :company_name, translation_options: @account.account_type %>
<% content_for :page_title, t('.title') %>
<%= ds_section_title(section_title: t('.section_title')) %>
<%= ds_paragraph{t('.description')} %>
<%= ds_form_with(model: @account, url: @post_path) do |f| %>
  <%= f.ds_fields_for(@account.company) do |c| %>
    <%= c.ds_text_field(method: :company_name, autocomplete: 'organization') %>
  <% end %>
<% end %>
