<%= content_for :hide_back_link, true %>
<% content_for :page_view, render_page_view(__FILE__) %>
<% content_for :page_title, t('error_title') %>
<% content_for :hide_menu_link,true %>
<%= render 'layouts/form_errors', error_hashes: nil %>
<%= button_tag t('close'), { :name => 'close', :class => 'scot-rev-button close', :id => 'close' } %>
