<% content_for(:page_title, t('.' + @claim_payment.translation_prefix + '_title')) %>
<%= ds_paragraph{ t('.' + @claim_payment.translation_prefix + '_address')} %>
<%= ds_form_with(model: @claim_payment, url: @post_path, method: :post, button_action: (@show_manual_address ? :continue : :none)) do |f| %>
  <%= ds_address_search(address_summary: @address_summary, search_results: @search_results , address: @address_detail,
      show_address_detail: @show_manual_address, readonly: @address_read_only) %>
<% end %>
