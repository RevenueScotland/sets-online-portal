<%= content_for :hide_back_link, true %>
<%= content_for :public_landing_return_link, t('cancel')%>
<% content_for :page_view, render_page_view(__FILE__) %>
<%= content_for :page_title, t('.title') %>
<%= display_paragraph_components(heading: t('.ads_heading'), body: t('.ads_description')) %>
<%= display_paragraph_components(heading: t('.eligibility_heading'), body: t('.eligibility_description')) %>
<%= display_paragraph_components(heading: t('.receive_repayment_heading'), body: t('.receive_repayment_description')) %>
<p><%= link_to(t('continue'), claim_claim_payments_eligibility_path, class: 'scot-rev-button') %></p>
