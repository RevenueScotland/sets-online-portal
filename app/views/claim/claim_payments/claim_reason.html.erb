<% content_for(:page_title, t('.title')) %>
<%= ds_form_with( model: @claim_payment, url: @post_path) do |f| %>
  <%= rs_conditional_visibility(visible_value: "OTHER") do |c| %>
    <% c.with_visibility_control_radio_group(builder: f.builder, method: :reason, options_list:@claim_payment.reason_list) %>
    <%= f.ds_text_area(method: :claim_desc) %>
  <% end %>
<% end %>
