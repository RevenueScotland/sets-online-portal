<% content_for(:page_title, t('.title')) %>
<%= ds_warning { t('.ads_warning', link: ds_link(url: external_link_url('LBTT_ADS_DISPOS'), name: t('.lbtt_ads_link'),target: '_blank')).html_safe } %>
<% content_for(:navigation_link, request.path) %>
<%= ds_form_with(model: @claim_payment, url: @post_path, method: :post) do |f| %>
	<%= f.ds_radio_group( method: :effective_date_checker, options_list: @claim_payment.list_ref_data(:effective_date_checker)) %>
<% end %>
