<% content_for(:page_title, t('.' + @claim_payment.translation_prefix + '_title')) %>
<%= ds_form_with(model: @claim_payment, url: @post_path ) do |f| %>  
  <%= f.ds_date_picker(method: :date_of_sale) %>
<%end%>
