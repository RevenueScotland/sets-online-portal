<% content_for(:page_title, t('.title')) %>
<%= ds_form_with(model: @property, url: @post_path, method: :post) do |f| %>
  <%= f.ds_radio_group(method: :ads_due_ind, options_list: @property.list_ref_data(:ads_due_ind),
                                        interpolations: { link:
                                                    ds_link(name: t('.ads_due_ind_link'), url: external_link_url('LBTT_ADS'), target: '_blank') }, alignment: :horizontal) %>
<% end %>
