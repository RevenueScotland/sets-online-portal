<% content_for(:page_title, t('.'+@party.party_type+ '_title')) %>
<%= ds_form_with(model: @party, url: @post_path, method: :post) do |f| %>
  <%= f.ds_radio_group(method: :is_acting_as_trustee, 
        options_list: @party.list_ref_data(:is_acting_as_trustee),alignment: :horizontal,
        interpolations: {link: ds_link(url: external_link_url('REP_PARTNER'), name: t('.is_acting_as_trustee_partnership'), target: '_blank') } ) %>
<% end %>
