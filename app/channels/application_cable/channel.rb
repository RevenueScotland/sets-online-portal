# frozen_string_literal: true

# Base application cable module for websocket
module ApplicationCable
  # Base application level channel for cable
  class Channel < ActionCable::Channel::Base
  end
end
